;(function () {
    "use strict";
    const minValue = prompt("Enter min value:", "5");
    let maxValue = prompt("Enter max value:", "8");
    /*let counter = 2;*/
    let isPrime = false;

    for(let i = 0; i < (maxValue - minValue); i++) {
        for (let k = 0; k < +minValue + k)
        if(Number.isInteger(minValue + i) === false) {

        }
    }
}) ();