; (function () {
    "use strict";
    const answer = prompt("What is an official name of JavaScript?");

    if(answer === "ECMAScript") {
        alert("You are right!");
    }
    else {
        alert("Don't you know? ECMAScript!");
    }
}) ();
