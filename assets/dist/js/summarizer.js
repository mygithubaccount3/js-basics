; (function () {
    "use strict";
    const first = Number(prompt("Enter first argument:", "5"));
    const second = Number(prompt("Enter second argument:", "3"));
    const sum = first + second;
    alert("Sum is: " + sum);
}) ();
